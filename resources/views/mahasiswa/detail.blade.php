@extends('layouts.app')

@section('content')
    <section class="rounded-[2rem] bg-gradient-to-br from-[#062f3d] to-[#0d6b6b] p-8 text-white shadow-xl sm:p-12"><div class="flex flex-col gap-8 sm:flex-row sm:items-center"><div class="grid h-28 w-28 place-items-center rounded-3xl bg-white/15 text-4xl font-black ring-1 ring-white/30">HS</div><div><p class="font-mono text-sm text-teal-200">{{ $mahasiswa['nrp'] }}</p><h1 class="mt-2 text-4xl font-black">{{ $mahasiswa['nama'] }}</h1><p class="mt-3 max-w-2xl leading-7 text-teal-50">{{ $mahasiswa['bio'] }}</p><div class="mt-6 flex flex-wrap gap-3"><a href="{{ $mahasiswa['instagram'] }}" target="_blank" rel="noopener" class="rounded-full bg-white px-4 py-2 text-sm font-bold text-teal-800 transition hover:-translate-y-1 hover:shadow-lg">◎ Instagram</a><a href="{{ $mahasiswa['linkedin'] }}" target="_blank" rel="noopener" class="rounded-full border border-white/40 px-4 py-2 text-sm font-bold transition hover:bg-white/10">in LinkedIn</a></div></div></div></section>
    <section class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach (['Program Studi' => $mahasiswa['prodi'], 'Angkatan' => $mahasiswa['angkatan'], 'IPK' => $mahasiswa['ipk'], 'Minat' => $mahasiswa['minat']] as $label => $value)<article class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-100"><p class="text-xs font-bold uppercase tracking-wider text-slate-400">{{ $label }}</p><p class="mt-2 font-bold leading-6 text-slate-800">{{ $value }}</p></article>@endforeach</section>
    <section class="mt-12"><p class="text-sm font-bold uppercase tracking-widest text-teal-700">Journey</p><h2 class="mt-1 text-3xl font-black text-slate-900">Experience timeline</h2><div class="mt-7 space-y-5 border-l-2 border-teal-200 pl-6">@foreach ($mahasiswa['pengalaman'] as $pengalaman)<article class="relative rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-100"><span class="absolute -left-[2.05rem] top-8 h-4 w-4 rounded-full border-4 border-slate-50 bg-teal-600"></span><p class="text-sm font-bold text-teal-700">{{ $pengalaman['periode'] }}</p><h3 class="mt-1 text-xl font-black text-slate-900">{{ $pengalaman['peran'] }}</h3><p class="mt-1 font-medium text-slate-500">{{ $pengalaman['organisasi'] }}</p><ul class="mt-4 space-y-2 text-sm leading-6 text-slate-600">@foreach ($pengalaman['poin'] as $poin)<li>✦ {{ $poin }}</li>@endforeach</ul></article>@endforeach</div></section>
    <section class="mt-12"><p class="text-sm font-bold uppercase tracking-widest text-teal-700">Toolkit</p><h2 class="mt-1 text-3xl font-black text-slate-900">Skills &amp; strengths</h2><div class="mt-6 grid gap-5 md:grid-cols-2">@foreach ($mahasiswa['skills'] as $kategori => $skills)<article class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-100"><h3 class="font-bold text-slate-800">{{ $kategori }}</h3><div class="mt-4 flex flex-wrap gap-2">@foreach ($skills as $skill)<span class="rounded-full bg-teal-50 px-3 py-1.5 text-sm font-semibold text-teal-800 transition hover:scale-105 hover:bg-teal-100">{{ $skill }}</span>@endforeach</div></article>@endforeach</div></section>
@endsection
